name: Build Android APK
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4 # تحديث للإصدار الرابع

      - name: Set up JDK 17
        uses: actions/setup-java@v4 # تحديث للإصدار الرابع
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Node.js
        uses: actions/setup-node@v4 # تحديث للإصدار الرابع
        with:
          node-version: '18'

      - name: Install Dependencies
        run: |
          npm install
          npm install @capacitor/core @capacitor/android @capacitor/cli

      - name: Build Web Assets
        run: npm run build

      - name: Build Android APK
        run: |
          npx cap add android || true
          npx cap sync
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4 # هذا هو التصحيح الجوهري لرسالة الخطأ
        with:
          name: Novexa-Final-App
          path: android/app/build/outputs/apk/debug/app-debug.apk
